apiVersion: v1
kind: Service
metadata:
  name: {{ include "ruby-test-apps.fullname" . }}
  labels:
    {{- include "ruby-test-apps.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ruby-test-apps.selectorLabels" . | nindent 4 }}
